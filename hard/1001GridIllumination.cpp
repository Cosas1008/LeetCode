/*
1 <= N <= 10^9
0 <= lamps.length <= 20000
0 <= queries.length <= 20000
lamps[i].length == queries[i].length == 2
*/
#include <vector>

using namespace std;

class Solution {
public:
    vector<int> gridIllumination(int N, vector<vector<int>>& lamps, vector<vector<int>>& queries) {
        
    }
};

int main(void)
{
    Solution s;
    N = 10;
    vector<int> innerLamp, innerQ;
    vector<vector<int>> outterLamp, outterQ;
    
    s.gridIllumination(outterLamp,outterQ);
}