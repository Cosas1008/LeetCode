#include <iostream>

using namespace std

int main()
{
    int tc, nu, m;
    cin >> tc >> nu >> m;
    int ans = 0;
    for(int t=0; t<tc; t++)
    {
        int a[nu];
        for(int n = 0; n <nu; n++)
        {
            cin >> a[n]; 
        }
    }
    cout << "Case #" << t+1 << ": " << ans << endl;
    return 0;
}